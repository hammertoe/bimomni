Operator-curated sample clips. Add files here (≤29 s, 16 kHz mono PCM_16 WAV):

| Label | Expected file |
|---|---|
| Itinerary (TikTok) | `itinerary_29s.wav` |
| Harbour Lights (TikTok) | `harbour_lights_29s.wav` |
| HOTT 95.3 broadcast | `radio_hott_29s.wav` |
| VOB 92.9 broadcast | `radio_vob_29s.wav` |
| CBC news extract | `radio_cbc_news_29s.wav` |

The launcher filters the dropdown to clips that actually exist, so a partial
bundle is fine — the demo continues to work for user-uploaded audio either way.
